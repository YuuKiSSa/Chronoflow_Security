apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: task-service
  namespace: chronoflow
spec:
  selector:
    matchLabels:
      app: task-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
