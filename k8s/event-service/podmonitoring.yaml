apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: event-service
  namespace: chronoflow
spec:
  selector:
    matchLabels:
      app: event-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
