apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: user-service
  namespace: chronoflow
spec:
  selector:
    matchLabels:
      app: user-service
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s
